{
  "name": "backend",
  "version": "1.0.0",
  "description": "Fastify backend for the Angular app",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "dev": "NODE_ENV=development node --watch src/server.js",
    "start": "NODE_ENV=production node src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Fer Falcon",
  "license": "MIT",
  "dependencies": {
    "@fastify/cors": "^11.1.0",
    "fastify": "^5.6.1"
  },
  "devDependencies": {
    "pino-pretty": "^13.1.2"
  }
}
